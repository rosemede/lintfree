name: Lychee
formats:
  txt:
    syntax: regex
    rules:
      - match: '^([^:]+):(\d+):(.*)$'
        severity: warning
        reset: true
        set:
          filename: 0
          line: 1
          message: 2
